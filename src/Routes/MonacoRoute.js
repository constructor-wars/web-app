import React from "react";
import MonacoContainer from "../Containers/MonacoContainer";

class MonacoRoute extends React.Component {
  render() {
    return (
      <div>
        <MonacoContainer />
      </div>
    );
  }
}

export default MonacoRoute;
