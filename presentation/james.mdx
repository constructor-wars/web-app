import { Notes } from 'mdx-deck'
import Small from './Small'

# Editor Technologies

---

## Monaco Editor
#### VS Code editor in the browser

<img src="./monaco.gif"/>
<Notes>
    James
  Only visible in presenter mode
</Notes>

---

# Evaluating data 

#### Sending data via `postMessage()`


``` jsx
<script>
document.getElementById('targetFrame')
    .contentWindow.postMessage(dataToSendToIframe, '*')
</script>

```
<Notes>
    James
  Only visible in presenter mode
</Notes>

---


## Executing data 

#### Replying via `postMessage()`

``` jsx
<script>
    window.addEventListener('message', event => {
            eval(event.data.codeToEval);
            const evalWithTest = eval(functionName);
            result = evalWithTest.apply(null, sampleInput);
        event.source.postMessage(result, event.origin);
    });
</script>

```


<Notes>
We are executing the code in a sure html file
</Notes>
---
# Challenge 

planning and leading the project.



<Small name='james' quote='@ Rafal, dont copy and paste'/>

<Notes>
   Hi, now this is Julius...
</Notes>


