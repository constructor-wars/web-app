import { Notes } from 'mdx-deck'
import Small from './Small'

# technology

API routes
react redux and react router for dymanic

```jsx
 <Route
    exact
    path="/editor"
    render={() => <EditorContainer {...this.props.GITHUB_DATA} />}
    />
    <Route exact path="/about" render={() => <AboutRoute />} />
    <Route exact path="/syllabus" render={() => <SyllabusRoute />} />...

```

---
# Challenge 
Planning the datastructure more as we have to refactor the db better

---
